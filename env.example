# AWS AgentBedrock Configuration (Required)
AGENT_ID=your-agent-id
AGENT_ALIAS=your-agent-alias-id
AWS_REGION=us-east-1

# AWS Credentials (Choose ONE method)
# -----------------------------------------
# Method 1: Use AWS CLI credentials (~/.aws/credentials)
#   - Just run: aws configure
#   - Leave these empty, the SDK will find them automatically
#
# Method 2: Use AWS Profile
# AWS_PROFILE=your-profile-name
#
# Method 3: Use explicit credentials (not recommended for production)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
#
# Method 4: IAM Role (for EC2, ECS, Lambda)
#   - No credentials needed, SDK uses instance role automatically

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
DATABASE_NAME=agentbedrock

# Backend Configuration
PORT=8080
ALLOWED_ORIGINS=http://localhost:3000

# Excel Upload (Optional - for Excel file support via S3)
# If not set, Excel uploads will be disabled
LAMBDA_FUNCTION_NAME=agentic-ai-finance-dev-mcp-gateway

# Frontend Configuration
NUXT_PUBLIC_API_BASE=http://localhost:8081
